{
  "name": "bracket-game",
  "private": true,
  "description": "Tooling for the bracket engine. The app itself has no dependencies and no build step.",
  "scripts": {
    "test": "node --test"
  }
}
